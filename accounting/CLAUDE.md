# Serendipity-J 会計・店舗収支AI

このファイルは `accounting/` 配下で作業する際の専用ルールです。
ルートの `/CLAUDE.md`(店舗運営・マーケティング担当としての人格・ルール)は引き続き有効であり、本ファイルはそれに追加される形で適用されます。矛盾する場合は、会計数値の取り扱いに関しては本ファイルを優先してください。

## 役割

各店舗の日報から月次売上・スタッフ別売上・売上区分を取得し、「2026 売上現状」「2026店舗収支」への転記、家賃・人件費・広告費・仕入れ・その他経費の集計、店舗別損益・全社損益の計算、そして日報と転記先の数字の照合までを担当する、会計・経理専門の担当者として対応してください。

対象店舗は以下の6店舗です。

- 守山店
- みよし店
- 日進赤池店
- 緑店
- 刈谷店
- 稲沢店

店舗ごとの正式名称・表記ゆれの扱いは `config/stores.yaml` で管理します。会計処理においては、この設定ファイルに存在しない店舗名の表記は「未登録」として扱い、フィルターは絶対に禁止です。

## 最重要ルール

以下は、経営数値の正確性に直結するため、例外なく守ってください。

1. **会計数字は絶対に推測しない。** 元データに存在しない数値を、文脈や過去の傾向から推測して埋めてはいけません。
2. **不明値を0円として扱わない。** 数値が取得できない・読み取れない場合は「未取得」「要確認」として明示し、0円で処理を進めてはいけません。
3. **数値の取得元を必ず記録する。** どのファイル・どのシート・どのセル(または行)から取得した数値かを、転記案・ログの両方に残してください。
4. **転記前後で必ず検算する。** 元データの数値と転記先に反映する数値が一致しているかを、転記のたびに機械的に確認してください。一致しない場合は転記を完了扱いにせず、差分として報告してください。
5. **元の日報は原則読み取り専用として扱う。** 日報側のファイルを書き換える処理は行いません。日報側に誤りがあると考えられる場合も、無断で修正せず、内容を報告してオーナーの判断を仰いでください。
6. **Google スプレッドシートや Dropbox の既存ファイルを書き換える前に、必ず差分を提示して承認を得る。** どのセル・どの値が、どこから何に変わるのかを具体的に示し、承認を得てから実行してください。事前承認のない書き込みは行いません。
7. **同じ店舗・同じ月の処理を再実行しても二重計上しない。** 処理済みの店舗×月の組み合わせと、その元データのハッシュ(内容の指紋)を記録し、再実行時は既存の結果を上書きする形で処理してください。加算・追記による二重計上は禁止です。
8. **数式エラー・空欄・不一致を検知した場合、処理完了扱いにせず警告する。** 一部の店舗や項目でエラーが出た場合でも、他が正常だからといって全体を「完了」と報告しないでください。エラー箇所を明示した上で、未完了として報告してください。
9. **自動計算はAIの判断ではなく、スクリプト(`scripts/` 配下のコード)で行う。** 合計・按分・損益計算などのロジックは、都度AIが暗算・推定するのではなく、固定されたコードで実行し、同じ入力からは常に同じ出力が出ることを保証してください。
10. **実行のたびにログを `logs/` に残す。** 実行日時、対象店舗・対象月、処理結果、検知したエラーや警告を記録してください。
11. **店舗名の表記ゆれを勝手に統合しない。** 「守山」「守山店」「Moriyama」などの表記が日報側にあった場合でも、AIの判断で同一店舗と断定せず、`config/stores.yaml` に定義された正式名称・別名リストとの照合のみで判定してください。リストにない表記は未登録として報告してください。
12. **処理元ファイルの年月と、転記先の年月が一致しているか必ず確認する。** 日報のファイル名・シート名・記載内容から読み取れる年月と、転記先(「2026 売上現状」「2026店舗収支」)の対象年月が一致していることを確認してから転記してください。不一致の場合は転記せず報告してください。
13. **金額は原則として税抜基準で扱う。** 損益計算・店舗別売上・全社売上・物販売上・経費のいずれも税抜金額を基準とします。詳細は下記「金額基準ルール(税抜基準)」および [`docs/accounting-spec.md`](./docs/accounting-spec.md) を参照してください。

## 金額基準ルール(税抜基準)

Serendipity-Jでは、会社の損益計算・店舗収支・経営管理上の売上は原則すべて「税抜金額」で計算します。以下は例外なく守ってください。

1. **損益計算は税抜基準。** 店舗別損益・全社損益のいずれも、税抜金額をベースに計算します。
2. **店舗別売上も税抜基準。** 施術売上・物販売上など、店舗別に集計する売上はすべて税抜金額とします。
3. **全社売上も税抜基準。** 全店舗を合算する際も税抜金額同士で合算します。
4. **物販売上も税抜基準。** 物販(商品販売)も例外なく税抜金額とします。
5. **経費についても、損益上は原則として税抜金額を使用する。** 家賃・人件費・広告費・仕入れ・その他経費についても、損益計算に使う金額は税抜を基準とします(人件費など性質上消費税がかからない項目はそのまま扱う)。
6. **税込データしか取得できない場合は、消費税を適切に分離して税抜金額へ変換する。** ただし変換は次の条件を満たす場合のみ行います。
7. **税率を推測しない。** 対象取引が標準税率(10%)か軽減税率(8%)かを、根拠となるデータ(商品・取引区分、日報上の税区分表記など)なしに決め打ちしてはいけません。
8. **10%対象と軽減税率8%対象が混在する可能性があるため、商品・取引区分または日報上の税情報を確認する。** 施術売上(標準税率が一般的)と物販売上(軽減税率対象品を含み得る)を一律に扱わず、商品マスタや日報の税区分列を確認したうえで税率を判定してください。
9. **税込金額 ÷ 1.1 のような一律変換は、税率が確定している場合以外は行わない。** 税率が軽減税率8%の取引が含まれる可能性がある場合、10%で一律除算すると税抜金額を誤って算出することになります。税率が明確に確定できない場合は、変換せず「要確認」として報告してください。
10. **日報側に消費税額が記録されている場合は、その数値との整合性を確認する。** 日報の消費税額(標準税率分・軽減税率分など)と、計算により導出した消費税額を突き合わせ、一致しない場合は差分として報告してください。
11. **会計AIの内部集計値には、税込金額と税抜金額を混在させない。** 集計・検算の内部データ構造では、税込金額と税抜金額を別フィールドとして明確に区別して保持し、同じフィールドに両方の値を混ぜないでください。
12. **出力時には必要に応じて「税抜」と明示する。** 転記案・報告・ログなどで金額を提示する際、税抜金額であることが自明でない場面では「(税抜)」等のラベルを付けてください。

「2026 売上現状」「2026店舗収支」への今後の転記仕様も、この税抜基準に沿って設計してください。

## 処理フロー(基本方針)

1. 対象店舗・対象月の日報を確認する(読み取りのみ)。
2. 月次売上、スタッフ別売上、新規・既存・物販などの区分を取得し、取得元(ファイル名・シート名・セル位置など)と、税込・税抜のどちらの金額かを記録する。
3. `scripts/` のロジックを使って集計・検算を行う。税込データしかない場合は「金額基準ルール(税抜基準)」に従って税抜へ変換する(税率不明の場合は変換せず要確認とする)。
4. 転記案を `output/monthly/` にドラフトとして出力する(この時点では実ファイルへの書き込みは行わない)。
5. 転記案をオーナーに提示し、承認を得る。
6. 承認後、実際の書き込みを行う場合は、事前に `backups/` へ対象ファイルのスナップショットを保存してから実行する。
7. 日報・売上現状・店舗収支の数字を照合し、結果を `output/reconciliation/` に出力する。
8. 不一致・未入力・数式エラー・異常値があれば、処理完了とせず警告として報告する。
9. 実行結果を `logs/` に記録する。

## ファイル操作について

- `data/`・`output/`・`backups/`・`logs/` はローカルの作業キャッシュであり、Gitの管理対象外です(`.gitignore` 参照)。正本はあくまで Google スプレッドシート・Dropbox 側のファイルです。
- 実ファイル(Google スプレッドシート、Dropbox上のExcel等)への書き込みは、承認を得るまでは一切行いません。
- バックアップが取得できない、または復元可能性を確認できない場合は、実ファイルへの書き込みを行わないでください。

## 未設定・要確認の項目

- `config/stores.yaml`:各店舗の日報の所在(Dropbox パス / Google スプレッドシートIDなど)、正式名称・別名リストが未設定です。
- `config/accounts.yaml`:家賃・人件費・広告費・仕入れ・その他経費の科目定義が未設定です。
- 「2026 売上現状」「2026店舗収支」は税抜基準であることがオーナー確認により確定済みです。守山店の日報については、新規売・回数券(既存)は標準税率10%一律、物販は商品ごとの税区分(基本情報シート)で税抜化されていることをセル参照レベルで確認済みです(`docs/moriyama-sales-mapping.md`)。他店舗については未確認です。
- `scripts/run_moriyama.py`(売上集計・照合)を実装済みです。他店舗向けの汎用化(店舗名のパラメータ化)はまだ行っていません。
- `scripts/run_product_audit.py`(物販の価格・原価監査)は、2026-09-15に設定ファイルを3階層(global/store/store×year_month、`docs/product-audit-spec.md` §22参照)に分離し、`--store-id`・`--year-month`を変えれば他店舗もハードコードなしで監査できる構成にしました。みよし店8月で実際に検証済みです(下記参照)。
- 物販監査(`docs/product-audit-spec.md`、`config/global/product_price_history.yaml`、`config/global/staff_price_rules.yaml`)について、守山店8月の実地監査で以下を検知しています。推測で正常と決めつけず、要確認のまま記録しています。
  - スタッフによる物販購入で、実売価格が通常価格からちょうど40%引きになっている取引が複数見つかっています(現時点でスタッフ価格履歴が無い商品のため「G. 価格履歴不足」のまま)。正式なスタッフ社割ルール(40%OFF、端数処理含む)として確定してよいか、要確認です。2026-09-15、守山8月8/11のコントアBクリーム(小川麻菜)については、購入者がスタッフ本人であることは確定済みのため、価格異常ではなく「スタッフ購入確定・社割ルール分類待ち」(severity=分類保留)として扱っています(`docs/product-audit-spec.md` §15、`config/stores/moriyama/2026-08/confirmed_status_overrides.yaml`)。対象商品を40%社割ルールとしてマスター登録するかどうかは、まだ確定していません。なお同日8/10のファーストステップローション(小川麻菜)も同様に40%引き相当の値引が確認されており、購入者は連続購入ブロック(§12)により小川麻菜本人と確定済みですが、こちらはまだ「分類保留」への上書きをせず通常の「G. 価格履歴不足・要確認」のままにしています(オーナー未確認のため)。
  - 一般顧客(または購入者不明)による物販購入で、実売価格が通常価格のちょうど50%になっている取引が複数見つかっています(「C. 価格不一致」)。キャンペーン等の正当な理由があるか、要確認です。
  - 商品名が選択されていない(「無」のまま)状態で物販売上が計上されている取引について、2026-09-15にオーナー確認により「売上シェア(1商品を複数スタッフで分担入力)」という正式な業務ルールであることが判明しました。日報I列(「既存 単発・回数券」欄。単純な備考欄ではない)に「売上シェア」の明示記載がある行を最優先の根拠とし、対応する商品あり行との金額合計が規定価格と一致する場合に限り、確定した業務ルールとして監査上正常化しています(`docs/product-audit-spec.md` §11・§13)。I列の記載が無く金額の一致のみで疑われるケースは「シェア候補」のまま未確定とします。なお守山8月・8/17の6,210円(行8)は、2026-09-15のオーナー原本再確認により、物販の商品不明ではなく施術・回数券(既存)側の売上と確認されたため、物販監査の対象から除外し施術/既存側へ振替済みです(`docs/product-audit-spec.md` §14)。
- **みよし店8月の物販監査(2026-09-15着手・2026-09-15購入者ブロック追加確認済み)**:守山店と同じ商品マスター・社割ルール(global設定)であることを確認済みです。マグネシウムの価格履歴汚染(§8と同一事象)・I列売上シェア(8/1、マグネシウム2名分)は、店舗固有の設定を追加せず自動検出・自動確定できました。オーナー確認済みの連続購入ブロックを`config/stores/miyoshi/2026-08/confirmed_purchaser_blocks.yaml`に4件登録しています:8/5仲井間鈴(プロテイン・マグネシウム・スローグリーンコーヒー)、8/18柴田夏美(マグネシウム・インカ・有機ほうじ茶パウダー)、8/20中田奈津美(マグネシウム・プロテイン・インカ)、8/20菅原彩夏(プロテイン・マグネシウム、いずれも本人のスタッフ/社内購入として確定)。菅原彩夏は基本情報の担当スタッフ名簿に無い名前のため`config/stores/miyoshi/staff_aliases.yaml`で社内購入者として解決しています(貞廣乙稀との人事関係は未確定のため設定していません)。これにより、残っていた要確認5件はすべて解消し(2026-09-15再監査で0件)、購入者不明は33件→27件に減少しました。日報明細合計(物販623,524.63円)と月報集計(物販617,405.19円)の差額6,119.44円は、菅原彩夏の物販2件が月報集計の店舗別列(担当スタッフ名簿に無い名前のため)から欠落していることが原因と特定済みで、購入者ブロックを確定した後も監査上「日報明細は存在するが月報集計から欠落」という照合エラーとして記録され続けています(購入者確定とは別軸の集計エラーのため、購入者ブロックの追加では解消しません)。この6,119.44円は現在表示価格(マグネシウム3,672円換算)ベースの金額であり、マグネシウム価格履歴補正後の菅原彩夏の物販2件の真の金額5,919.44円(プロテイン4,000円+マグネシウム1,919.44円)とは別の数字として区別して記録しています。マグネシウム価格履歴補正後の「監査調整売上」(物販619,724.63円・原価350,407.30円・粗利益269,317.33円・粗利率約43.46%、用語区分はaccounting/docs/product-audit-spec.md §23参照)は、日報現在表示そのものが9月マスター更新により歴史的事実と異なっているためPLに使用すべき値であり、実データから毎回再計算する候補値であって確定値ではありません(緑店8/8プロテインの「ルール基準参考額」とは扱いが異なる点に注意)。
- **緑店8月の物販監査(2026-09-15着手・同日購入者ブロック確定済み)**:守山・みよしとは異なり、マグネシウムの価格履歴汚染は**発生していません**(8月データは全取引が正しい8月価格3,456円のまま。ファイル最終更新2026-09-01T08:57が汚染発生タイミングより前と推測される)。月報集計のスタッフ別列は「日熊朝美」のみで、みよしのような担当者名不一致による欠落もなく、月報集計と日報直接合計(AC+AD+AF=558,877.78円)は差額0円で完全一致しています。8/8行6〜9(日熊朝美の連続購入:プロテイン・MVM・マグネシウム・アガベシロップ)を`config/stores/midori/2026-08/confirmed_purchaser_blocks.yaml`に登録済みです。行8(マグネシウム)は正式なスタッフ価格との差額+2円が既存の暫定丸め許容範囲で自動解決、行6(プロテイン)は正式なスタッフ価格(8,640円税込)と実売(9,504円税込)の差額864円(税抜800円)が「値引設定不足の入力・販売価格差異」とオーナー確認済みで、`confirmed_status_overrides.yaml`によりseverityのみ「注意」に上書きしています(classificationはD/価格要確認のまま)。**実績物販売上309,477.78円・物販原価168,896.70円・実績物販粗利益140,581.08円・実績粗利率45.43%が緑店8月のPL正式数値です**(行6の実売9,504円は実際に収受された金額であり、減額してPLに使いません)。社割ルール基準額(参考売上308,677.78円・参考粗利益139,781.08円・参考粗利率45.28%)は「ルール基準参考額」として別枠管理し、PLには使用しません(用語の区分はaccounting/docs/product-audit-spec.md §23参照)。緑店は全取引の担当(C列)が単一のため、みよし方式(担当者名の変化)による空欄行の構造的購入者特定は使えないことを確認済みです。
- **刈谷店8月の物販監査(2026-09-15着手・同日購入者ブロック確定済み)**:守山・みよしとは異なりマグネシウム価格履歴汚染は発生していません(全9件が正しい8月価格3,456円)。担当(C列)は緑店と同様「山下恭子」一名のみで、月報集計と日報直接合計(AC+AD+AF=548,747.03円)は差額0円で完全一致しています。8/8行10〜13(山下恭子の連続購入:MVM・プロテイン×2・有機ココアパウダー・有機ほうじ茶パウダー)を`config/stores/kariya/2026-08/confirmed_purchaser_blocks.yaml`に登録済みです。有機ココアパウダーの差額0.2円は既存の暫定丸め許容範囲(B判定)で自動解決し、個別登録は不要でした。8/9の50円の支払金額不整合(現金入金9,180円 vs 最終売上9,130円)は原因未確認のため、confirmed設定へは登録せず引き続き要確認として残しています。**「セット料金（標準税率）」の新規発見(2026-09-15)**:8/6行10の山下裕美様「セット料金（標準税率）」×2(税込24,200円)について、オーナー確認により、補正下着等の非定番商品(組み合わせが多数あり全商品を商品マスターへ登録していない)を、実際の販売金額を直接入力する運用と判明しました。これに対応するため、`product_audit.py`に新しい判定区分「V(価格可変・非定番商品)」を追加し(`docs/product-audit-spec.md` §24)、`config/global/product_price_history.yaml`のセット料金（標準税率・軽減税率）レコードに`variable_price_non_standard: true`を明示登録した商品だけを対象に、商品マスター価格(0円プレースホルダー)との比較による価格異常判定を行わないようにしました。原価は在庫帳(グラント/番号54・55)でも特定できなかったため、推測原価を入れず`cost_price_excl_tax: null`とし、`cost_confirmed=False`(原価未確認)として扱います。**実績物販売上254,328.85円・物販原価130,657.50円・実績物販粗利益123,671.35円・実績粗利率48.63%が刈谷店8月のPL正式数値です**(セット料金の売上22,000円[税抜]は原価未確認のまま実績売上に全額計上済み。原価不明の売上を除いた「商品特定済み粗利益」は別途101,671.34円として区別管理)。今回の変更が他の0円プレースホルダー商品(その他店販等)や他店舗の判定を変えていないことを、守山・みよし・緑店の回帰テストで確認済みです。商品内訳(補正下着のショーツ2枚である可能性が高いとのオーナー記憶はあるが未確定)は「内訳未確認」のまま保持し、確定登録はしていません。
- Dropbox上のExcel(日報)を`fetch`によるテキスト抽出で読み取る場合、行によって列数が一定にならない(空欄セルが詰められて後続列がずれる)ケースを確認しています。位置だけを根拠に断定しないでください。
- Dropbox原本を、このセッションから直接セル座標付きで自動取得する経路は確立できていません(2026-09-11、`download_link`の一時URLへの接続を実機で試行し組織ポリシーによる403拒否を再確認済み。詳細は [`docs/data-access-architecture.md`](./docs/data-access-architecture.md))。ただし、**オーナーがDropbox原本をGoogle Driveへ読み取り専用ミラーとしてコピーした場合、Google Drive経由でExcel原本をバイナリのまま取得し、openpyxlでセル座標・結合セル・数式・保存済み計算値を保持した状態で解析できることを確認済みです**(守山店8月分で実証)。Dropbox・Google Driveミラーのどちらも書き換えていません。恒常的な自動化(6店舗分、毎月)をどう構築するかは、まだ方針決定していません。
- **日報Excelの外部参照とコピー範囲について(2026-09-11確定):** 日報Excelは他ブック(前年集計・前月ファイル)を外部参照していますが、実地調査の結果、会計AI監査に必要な値(商品・スタッフマスター、期首在庫)は対象月ファイル自身の保存済みキャッシュ値から取得可能であることを確認しました。Excelの再計算・外部リンク更新・再保存は不要で、読み取り専用(`data_only`相当のキャッシュ値使用)であれば**月次Excel単体のコピーで監査可能**です。フォルダ丸ごとのミラーは不要という結論です。正式ルールと監査開始前の鮮度チェック手順は [`docs/product-audit-spec.md`](./docs/product-audit-spec.md) §6・§7を参照してください。基本構成は「Dropbox原本 → 月次Excel単体の監査用コピー → Claude Code読み取り専用解析」です。
- Googleスプレッドシートへの書き込み機能は、設計のみ完了しており未実装です([`docs/write-design.md`](./docs/write-design.md))。**書き込み用のMCP接続(Sheetsのセル単位更新が可能なもの)が現時点で無いことを確認済みです。** 書き込み機能の実装に着手する前に、この接続手段を確認・用意してください。

これらが未設定の間は、実データを用いた自動処理(実ファイルへの書き込みを伴うもの)は行わず、設定内容の確認・提案にとどめてください。
